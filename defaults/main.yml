---
# defaults file for pyenv
pyenv_path: "{{ ansible_env.HOME }}/pyenv"
pyenv_owner: "{{ ansible_env.USER }}"
pyenv_runcom: "~/.bashrc"
pyenv_update_git_install: no
pyenv_python_versions:
  - 3.5.1
pyenv_virtualenvs:
  - { venv_name: "latest", py_version: "3.5.1" }
